#!/usr/bin/python3

import sqlite3

# create db
db = sqlite3.connect("ttf.db").cursor()

# create table
db.execute("CREATE TABLE scoreboard(player_id, player_name, time, partner_id, flag, verified, banned)")
db.execute("CREATE TABLE gamestate(key, value)")

print("Database Ready")